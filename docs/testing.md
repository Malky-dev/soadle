# Testing

## Purpose

The project favors simple, maintainable, and business-oriented tests.

The main goal is to cover:

- business logic in `Application`
- domain rules in `Domain`

Coverage is not a goal by itself. It is used to identify gaps in critical behavior.

---

## Running tests

From the project root:

```bash
php tests/run.php